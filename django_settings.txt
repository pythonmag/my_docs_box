
"""
Django settings for udemy_blog project.

Generated by 'django-admin startproject' using Django 3.0.3.

For more information on this file, see
https://docs.djangoproject.com/en/3.0/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/3.0/ref/settings/
"""





about: root, redis(cach), django-environ
=========================
https://djangostars.com/blog/configuring-django-settings-best-practices/        ## maybe ???


1) app
===============
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',              # пример готовой вложенной аппликации
    'django.contrib.staticfiles',
    'first_app',                            # пример кастомной аппликации
    'second_app',
]


2)
=============
import os
from . import secret_settings

## secret_settings.py   >>>     .gitignore befor git commit


SECRET_KEY = secret_settings.SECRET_KEY



2) static files
================

# Build paths inside the project like this: os.path.join(BASE_DIR, ...)

BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
TEMPLATE_DIR = os.path.join(BASE_DIR, 'templates')
STATIC_ROOT = os.path.join(BASE_DIR, '..', 'static_all')
MEDIA_ROOT = os.path.join(BASE_DIR, '..', 'media')
STATICFILES_DIR = os.path.join(BASE_DIR, 'static')


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/3.0/howto/static-files/

STATIC_URL = '/static/'
MEDIA_URL = '/media/'




